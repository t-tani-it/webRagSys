[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
extend-select = ["UP", "S", "BLE", "E", "F"]
ignore = ["UP009", "B008"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]
